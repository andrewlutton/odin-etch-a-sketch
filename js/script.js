const grid = document.querySelector(".grid-container");
const gridTotalSize = 960; //total grid size, square, px

grid.setAttribute("style",`height: ${gridTotalSize}px; width: ${gridTotalSize}px; background-color: black`);

